import spinner from "../../images/spinner.gif";
const Spinner = () => {
  return (
    <img
      src={spinner}
      alt="spinner"
      style={{
        marginLeft: "330px",
      }}
    />
  );
};

export default Spinner;
